# Information Security Management Exam Bot


This is a minimal FastAPI application using OpenAI's GPT API. Set the `OPENAI_API_KEY` environment variable and run:

```bash
pip install -r requirements.txt
uvicorn bot.app:app --reload
```
